project_name: usque
builds:
  - id: android
    goos:
      - android
    goarch:
      - arm64
    goarm:
      - 5
      - 6
      - 7
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: &ldflags
      - "-s -w"
      - "-X {{ .ModulePath }}/cmd.version={{.Tag}}"
      - "-X {{ .ModulePath }}/cmd.commit={{.Commit}}"
      - "-X {{ .ModulePath }}/cmd.date={{.Date}}"
  
  - id: linux-amd64
    goos:
      - linux
    goarch:
      - amd64
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: *ldflags

  - id: linux-arm
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - 5
      - 6
      - 7
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: *ldflags

  - id: linux-arm64
    goos:
      - linux
    goarch:
      - arm64
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: *ldflags

  - id: linux-mips
    goos:
      - linux
    goarch:
      - mips
      - mipsle
      - mips64
      - mips64le
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: *ldflags

  - id: windows
    goos:
      - windows
    goarch:
      - arm64
      - amd64
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: *ldflags

  - id: darwin
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags: *ldflags

archives:
  - formats:
    - zip
    name_template: "usque_{{.Version}}_{{.Os}}_{{.Arch}}{{if .Arm}}v{{.Arm}}{{end}}"
    files:
      - LICENSE.md
      - README.md

checksum:
  name_template: "checksums.txt"
